# Copy this file to .env and fill in values before running docker compose.

# Optional: Anthropic API key for server-side AI analysis.
# Users can also supply their own key via the UI — this is not required.
ANTHROPIC_API_KEY=
